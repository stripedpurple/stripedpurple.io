<div class="select-none py-4 px-6 flex items-center justify-center m-0 w-full z-20 absolute top-0" id="navbar">
  <nav
  class="flex justify-center rounded-full items-center gap-6 sm:gap-8 px-6 py-2 text-lg font-medium capitalize backdrop-blur-sm bg-primary-800/50"
>
  <a href="#" class="link">Home</a>
  <a href="#whatIDo" class="link">What I do</a>
  <a href="#projectHighlight" class="link">Projects</a>
  <a href="#career" class="link">Career</a>
  <a href="#footer" class="link">contact</a>
</nav>
</div>

<style>
  a::before {
    font-size: 1rem;
    text-align: center;
    vertical-align: middle;
  }
</style>

<script defer>
  window.addEventListener("load", () => {
    const obserables = document.getElementById("scrollTo");
    const navbar = document.getElementById("navbar");
    const io = new IntersectionObserver((entries) => {
      entries.forEach(
        (entry) => {
          if (!entry.isIntersecting) {
            navbar.classList.add(
              "fixed",
            );
            navbar.classList.remove("top-0", "absolute");
          } else {
            navbar.classList.remove(
              "fixed",
            );
            navbar.classList.add("top-0", "absolute");
          }
        },
        { threshold: 0 }
      );
    });

    io.observe(obserables);
  });
</script>
